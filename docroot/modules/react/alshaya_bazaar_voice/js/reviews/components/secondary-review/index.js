import React from 'react';

const SecondaryReview = ({
  SecondaryAttribute,
}) => (
  <div className="secondary-attribute">
    <div className="attribute-list">{SecondaryAttribute}</div>
  </div>
);

export default SecondaryReview;
