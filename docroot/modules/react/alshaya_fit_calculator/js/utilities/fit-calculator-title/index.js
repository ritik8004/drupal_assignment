import React from 'react';

const FitCalculatorTitle = ({ children, className = '' }) => <div className={`fit-calculator-section-title ${className}`}>{children}</div>;

export default FitCalculatorTitle;
