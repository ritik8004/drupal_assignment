import React from 'react';

const StyleFinderSubTitle = ({ children, className = '' }) => <div className={`style-finder-sub-title ${className}`}>{children}</div>;

export default StyleFinderSubTitle;
