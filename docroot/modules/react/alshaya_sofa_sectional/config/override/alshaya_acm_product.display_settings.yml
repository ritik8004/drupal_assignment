swatches:
  pdp:
    - configuration
    - fabric_color
# Added swatch attibute specific media roles like
# 'swatch_image' or 'configuration_attr_image'.
swatches_roles:
  configuration: 'configuration_attr_image'
# Exclude configuration swatch images from the image gallery.
media_roles_to_hide_in_gallery: 'thumbnail,swatch_image,configuration_attr_image'
