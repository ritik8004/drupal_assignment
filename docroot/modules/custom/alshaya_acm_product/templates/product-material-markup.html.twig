{% if material is not empty %}
  <div class="material-wrapper">
    <div class="material-label">{{ title }}</div>
    <div class="material-value">
      <p>{{ material }}</p>
    </div>
  </div>
{% endif %}
