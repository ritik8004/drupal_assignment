<?php

/**
 * @file
 * Install file.
 */

/**
 * Implements hook_modules_installed().
 */
function alshaya_modal_modules_installed(array $modules) {
  if (in_array('alshaya_modal', $modules)) {
    module_set_weight('alshaya_modal', 100);
  }
}
