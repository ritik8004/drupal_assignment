langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_product_slug
    - node.type.rcs_product
id: node.rcs_product.field_product_slug
field_name: field_product_slug
entity_type: node
bundle: rcs_product
label: Slug
description: 'Every slug must be unique across a project, but a product can have the same slug for different languages. Allowed are alphabetic, numeric, underscore (_) and hyphen (-) characters. Maximum size is 256.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
