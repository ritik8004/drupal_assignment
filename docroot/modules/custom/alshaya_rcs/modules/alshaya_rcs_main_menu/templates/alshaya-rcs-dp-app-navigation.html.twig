{% if data %}
  {% if data.item %}
    <div class="l2-terms menu-terms-link">
      <ul>
        {% for path, name in data.item %}
          <li>
            <a href={{ path }}>{{ name }}</a>
          </li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  {% if data.item %}
    <div class="l3-terms menu-terms-link">
      <ul>
        {% for path, name in data.item %}
          <li>
            <a href={{ path }}>{{ name }}</a>
          </li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}
{% endif %}
