alshaya_acm_checkout.settings:
  type: config_object
  label: 'Checkout settings'
  mapping:
    checkout_guest_summary:
      type: text_format
      label: 'Checkout as guest summary'
    checkout_guest_email_usage:
      type: text_format
      label: 'Guest email usage description'
    checkout_guest_login:
      type: text_format
      label: 'Checkout login help'
    checkout_terms_condition:
      type: text_format
      label: 'Checkout Terms and Conditions'
    checkout_customer_service:
      type: text_format
      label: 'Checkout Customer Service'
    click_collect_method:
      type: string
      label: 'Click and Collect delivery method code'
    payments_default:
      type: string
      label: 'Default Payment method'
    payments_home_delivery:
      type: string
      label: 'Payment methods - Home Delivery'
    payments_click_collect:
      type: string
      label: 'Payment methods - Click and Collect'
