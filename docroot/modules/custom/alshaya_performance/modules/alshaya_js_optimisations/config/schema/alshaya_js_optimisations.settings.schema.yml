alshaya_js_optimisations.settings:
  type: config_object
  label: 'JS Performance Settings'
  mapping:
    enable_uglification:
      type: boolean
      label: 'Enable JS Uglification'
    critical_js:
      type: mapping
      label: 'Critical JS Settings'
      mapping:
        enabled:
          type: boolean
          label: 'Enable Critical JS'
        ie_only:
          type: string
          label: 'IE Only Scripts'
        site_libraries:
          type: string
          label: 'Site Basic Scripts'
        critical:
          type: string
          label: 'Critical Scripts'
        sitewide_1:
          type: string
          label: 'Sitewide Scripts 1'
        sitewide_2:
          type: string
          label: 'Sitewide Scripts 2'
        hidden_data:
          type: string
          label: 'Resolved libries data with weights'
