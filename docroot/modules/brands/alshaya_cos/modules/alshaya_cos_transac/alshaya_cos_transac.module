<?php

/**
 * Implements hook_rcs_placeholders_graphql_query_alter().
 */
function alshaya_cos_transac_rcs_placeholders_graphql_query_alter(array &$queries) {
  // Add style code for catalogue restructuring.
  array_push($queries['products']['items'], 'style_code');
}
