diff --git a/docroot/.htaccess b/docroot/.htaccess
index d20700cc1..5b09272ae 100644
--- a/docroot/.htaccess
+++ b/docroot/.htaccess
@@ -69,6 +69,14 @@ AddEncoding gzip svgz
 <IfModule mod_rewrite.c>
   RewriteEngine on
 
+  # Add maintenance page.
+  RewriteCond %{DOCUMENT_ROOT}/sites/g/files/%{HTTP_HOST}/maintenance.html -f
+  RewriteCond %{DOCUMENT_ROOT}/.maintenance -f
+  RewriteCond %{SCRIPT_FILENAME} !maintenance.html
+  RewriteRule ^.*$ /sites/g/files/%{HTTP_HOST}/maintenance.html [R=503,L]
+  ErrorDocument 503 /sites/g/files/%{HTTP_HOST}/maintenance.html
+  Header Set Cache-Control "max-age=0, no-store"
+
   # Rewrite rule to remove .html and remove kwt-* from url.
   RewriteRule ^kwt-([a-z]{2})/(.*).html$ /$1/$2 [R=301,L]
 
