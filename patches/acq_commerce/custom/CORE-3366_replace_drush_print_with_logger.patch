diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_promotion/acq_promotion.drush.inc b/docroot/modules/commerce/acq_commerce/modules/acq_promotion/acq_promotion.drush.inc
index 67592f68e..f5b5ea5b0 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_promotion/acq_promotion.drush.inc
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_promotion/acq_promotion.drush.inc
@@ -31,17 +31,15 @@ function acq_promotion_drush_command() {
  */
 function drush_acq_promotion_sync_commerce_promotions() {
   if ($types = drush_get_option('types')) {
-    drush_print(dt('Synchronizing all @types commerce promotions, this usually takes some time...', ['@types' => $types]));
+    \Drupal::logger('acq_promotion')->notice('Synchronizing all @types commerce promotions, this usually takes some time...', ['@types' => $types]);
     $types = explode(',', $types);
     $types = array_map('trim', $types);
     \Drupal::service('acq_promotion.promotions_manager')->syncPromotions($types);
   }
   else {
-    drush_print(dt('Synchronizing all commerce promotions, this usually takes some time...'));
+    \Drupal::logger('acq_promotion')->notice('Synchronizing all commerce promotions, this usually takes some time...');
     \Drupal::service('acq_promotion.promotions_manager')->syncPromotions();
   }
 
   \Drupal::logger('acq_promotion')->notice('Promotion sync completed.');
-
-  drush_print(dt('Done.'));
 }
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.drush.inc b/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.drush.inc
index d220b501a..bd66ef384 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.drush.inc
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.drush.inc
@@ -201,11 +201,10 @@ function drush_acq_sku_sync_commerce_cats() {
  * Implements drush product attribute sync command.
  */
 function drush_acq_sku_sync_commerce_product_options() {
-  drush_print(dt('Synchronizing all commerce product options, please wait...'));
+  \Drupal::logger('acq_sku')->notice('Synchronizing all commerce product options, please wait...');
   $container = \Drupal::getContainer();
   $container->get('acq_sku.product_options_manager')->synchronizeProductOptions();
   \Drupal::logger('acq_sku')->notice('Product attribute sync completed.');
-  drush_print(dt('Done.'));
 }
 
 /**
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku_position/acq_sku_position.drush.inc b/docroot/modules/commerce/acq_commerce/modules/acq_sku_position/acq_sku_position.drush.inc
index 313a809d0..963e555c9 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku_position/acq_sku_position.drush.inc
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku_position/acq_sku_position.drush.inc
@@ -32,7 +32,7 @@ function acq_sku_position_drush_command() {
  * Sync product position based on category.
  */
 function drush_acq_sku_position_position_sync($position_type = 'position') {
-  drush_print(dt('Product position sync in progress...'));
+  \Drupal::logger('acq_sku_position_position')->notice('Product position sync in progress...');
 
   $database = \Drupal::database();
 
@@ -53,28 +53,28 @@ function drush_acq_sku_position_position_sync($position_type = 'position') {
       }
     }
     catch (\Exception $e) {
-      drush_print(dt('Exception while fetching position for category @name (tid: @tid). The category probably does not exist in commerce backend', [
+      \Drupal::logger('acq_sku_position_position')->error('Exception while fetching position for category @name (tid: @tid). The category probably does not exist in commerce backend', [
         '@name' => $term->name,
         '@tid' => $term->tid,
-      ]));
+      ]);
       continue;
     }
 
     // Skip sync if error is found in the response for a particular category.
     if (is_array($response) && isset($response['message'])) {
-      drush_print(dt('Error in position sync for @name (tid: @tid). Response: @message', [
+      \Drupal::logger('acq_sku_position_position')->error('Error in position sync for @name (tid: @tid). Response: @message', [
         '@name' => $term->name,
         '@tid' => $term->tid,
         '@message' => $response['message'],
-      ]));
+      ]);
       continue;
     }
 
     // Start product position sync for this category.
-    drush_print(dt('Product position sync for @name (tid: @tid) in progress...', [
+    \Drupal::logger('acq_sku_position_position')->notice('Product position sync for @name (tid: @tid) in progress...', [
       '@name' => $term->name,
       '@tid' => $term->tid,
-    ]));
+    ]);
 
     // Delete existing records of position for this category.
     $database->delete('acq_sku_position')
@@ -124,5 +124,5 @@ function drush_acq_sku_position_position_sync($position_type = 'position') {
   // Allow other modules to take action after position sync finished.
   \Drupal::moduleHandler()->invokeAll('acq_sku_position_sync_finished');
 
-  drush_print(dt('Product position sync completed!'));
+  \Drupal::logger('acq_sku_position_position')->notice('Product position sync completed!');
 }
