diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php b/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php
index 146681b..1f2e3d9 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php
@@ -139,7 +139,12 @@ public function addItemToCart($sku, $quantity) {
 
       if ($item['sku'] == $sku) {
         $new_qty = (int) $item['qty'] + (int) $quantity;
-        $this->updateItemQuantity($sku, $new_qty);
+        if ($new_qty > 0) {
+          $this->updateItemQuantity($sku, $new_qty);
+        }
+        else {
+          $this->removeItemFromCart($sku);
+        }
         return;
       }
     }
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
index b79bd15..488438b 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
@@ -224,7 +224,9 @@ public function addToCartSubmit(array &$form, FormStateInterface $form_state) {
       }
       catch (\Exception $e) {
         // Remove item from cart.
-        $cart->removeItemFromCart($tree_pointer->getSku());
+        $cart->addItemToCart($tree_pointer->getSku(), -$quantity);
+        $cart->addItemToCart($tree['parent']->getSKU(), -$quantity);
+
         // Dispatch event so action can be taken.
         $dispatcher = \Drupal::service('event_dispatcher');
         $event = new AddToCartErrorEvent($e);
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php
index 9f9912e..921a757 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php
@@ -98,8 +98,9 @@ public function addToCartSubmit(array &$form, FormStateInterface $form_state) {
     catch (\Exception $e) {
       // Remove item from cart.
       foreach ($skus as $sku => $quantity) {
-        $cart->removeItemFromCart($sku);
+        $cart->addItemToCart($sku, -$quantity);
       }
+
       // Dispatch event so action can be taken.
       $dispatcher = \Drupal::service('event_dispatcher');
       $event = new AddToCartErrorEvent($e);
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php
index a672f27..c3fc07a 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php
@@ -70,7 +70,8 @@ public function addToCartSubmit(array &$form, FormStateInterface $form_state) {
     }
     catch (\Exception $e) {
       // Remove item from cart.
-      $cart->removeItemFromCart($sku);
+      $cart->addItemToCart($sku, -$quantity);
+
       // Dispatch event so action can be taken.
       $dispatcher = \Drupal::service('event_dispatcher');
       $event = new AddToCartErrorEvent($e);
diff --git a/docroot/modules/custom/alshaya_acm/alshaya_acm.module b/docroot/modules/custom/alshaya_acm/alshaya_acm.module
index 10099fd..0215db9 100644
--- a/docroot/modules/custom/alshaya_acm/alshaya_acm.module
+++ b/docroot/modules/custom/alshaya_acm/alshaya_acm.module
@@ -374,8 +374,15 @@ function alshaya_acm_cart_ajax_quantity_callback(&$form, FormStateInterface $for
     $error_message = t('Sorry, we now only have @quantity of these available and we have updated your basket.', ['@quantity' => $quantity]);
   }
 
-  $cart->updateItemQuantity($sku, $quantity);
-  $cartStorage->updateCart();
+  try {
+    $cart->updateItemQuantity($sku, $quantity);
+    $cartStorage->updateCart();
+  }
+  catch (\Exception $e) {
+    $cart->addItemToCart($sku, -$quantity);
+    _acq_cart_remove_zero_quantity_item($cart);
+  }
+
   $newcart = $cartStorage->getCart();
 
   // Get cart items by sku.
diff --git a/patches/acq_commerce/mmcpa-1219-2.patch b/patches/acq_commerce/mmcpa-1219-2.patch
new file mode 100644
index 0000000..5879ca5
--- /dev/null
+++ b/patches/acq_commerce/mmcpa-1219-2.patch
@@ -0,0 +1,71 @@
+diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php b/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php
+index 146681b..1f2e3d9 100644
+--- a/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php
++++ b/docroot/modules/commerce/acq_commerce/modules/acq_cart/src/Cart.php
+@@ -139,7 +139,12 @@ public function addItemToCart($sku, $quantity) {
+ 
+       if ($item['sku'] == $sku) {
+         $new_qty = (int) $item['qty'] + (int) $quantity;
+-        $this->updateItemQuantity($sku, $new_qty);
++        if ($new_qty > 0) {
++          $this->updateItemQuantity($sku, $new_qty);
++        }
++        else {
++          $this->removeItemFromCart($sku);
++        }
+         return;
+       }
+     }
+diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
+index b79bd15..488438b 100644
+--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
++++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
+@@ -224,7 +224,9 @@ public function addToCartSubmit(array &$form, FormStateInterface $form_state) {
+       }
+       catch (\Exception $e) {
+         // Remove item from cart.
+-        $cart->removeItemFromCart($tree_pointer->getSku());
++        $cart->addItemToCart($tree_pointer->getSku(), -$quantity);
++        $cart->addItemToCart($tree['parent']->getSKU(), -$quantity);
++
+         // Dispatch event so action can be taken.
+         $dispatcher = \Drupal::service('event_dispatcher');
+         $event = new AddToCartErrorEvent($e);
+diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php
+index 9f9912e..921a757 100644
+--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php
++++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Grouped.php
+@@ -98,8 +98,9 @@ public function addToCartSubmit(array &$form, FormStateInterface $form_state) {
+     catch (\Exception $e) {
+       // Remove item from cart.
+       foreach ($skus as $sku => $quantity) {
+-        $cart->removeItemFromCart($sku);
++        $cart->addItemToCart($sku, -$quantity);
+       }
++
+       // Dispatch event so action can be taken.
+       $dispatcher = \Drupal::service('event_dispatcher');
+       $event = new AddToCartErrorEvent($e);
+diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php
+index a672f27..c3fc07a 100644
+--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php
++++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Simple.php
+@@ -70,7 +70,8 @@ public function addToCartSubmit(array &$form, FormStateInterface $form_state) {
+     }
+     catch (\Exception $e) {
+       // Remove item from cart.
+-      $cart->removeItemFromCart($sku);
++      $cart->addItemToCart($sku, -$quantity);
++
+       // Dispatch event so action can be taken.
+       $dispatcher = \Drupal::service('event_dispatcher');
+       $event = new AddToCartErrorEvent($e);
+diff --git a/docroot/modules/custom/alshaya_acm/alshaya_acm.module b/docroot/modules/custom/alshaya_acm/alshaya_acm.module
+index 10099fd..0215db9 100644
+--- a/docroot/modules/custom/alshaya_acm/alshaya_acm.module
++++ b/docroot/modules/custom/alshaya_acm/alshaya_acm.module
+@@ -374,8 +374,15 @@ function alshaya_acm_cart_ajax_quantity_callback(&$form, FormStateInterface $for
+     $error_message = t('Sorry, we now only have @quantity of these available and we have updated your basket.', ['@quantity' => $quantity]);
+   }
+ 
+-  $cart->updateItemQuantity($sku, $
\ No newline at end of file
