diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.install b/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.install
index 98a39eceb..6c7641bab 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.install
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/acq_sku.install
@@ -7,6 +7,7 @@
 
 use Drupal\node\Entity\Node;
 use Drupal\menu_link_content\Entity\MenuLinkContent;
+use Symfony\Component\Yaml\Yaml;
 
 /**
  * Implements hook_install().
@@ -44,3 +45,11 @@ function acq_sku_install() {
   ]);
 }
 
+/**
+ * Implements hook_update_N().
+ */
+function acq_sku_update_8001() {
+  $config_path = drupal_get_path('module', 'acq_sku') . '/config/install/acq_sku.configurable_weights.yml';
+  $data = Yaml::parse($config_path);
+  \Drupal::configFactory()->getEditable('acq_sku.configurable_weights')->setData($data)->save(TRUE);
+}
\ No newline at end of file
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/config/install/acq_sku.configurable_weights.yml b/docroot/modules/commerce/acq_commerce/modules/acq_sku/config/install/acq_sku.configurable_weights.yml
new file mode 100644
index 000000000..6825b16b6
--- /dev/null
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/config/install/acq_sku.configurable_weights.yml
@@ -0,0 +1,2 @@
+weights:
+  size: 1
\ No newline at end of file
diff --git a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
index a61c97852..6c035e983 100644
--- a/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
+++ b/docroot/modules/commerce/acq_commerce/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
@@ -43,6 +43,7 @@ public function addToCartForm(array $form, FormStateInterface $form_state, SKU $
     ];
 
     $configurables = unserialize($sku->field_configurable_attributes->getString());
+    $configurable_weights = \Drupal::service('config.factory')->get('acq_sku.configurable_weights')->get('weights');
 
     foreach ($configurables as $configurable) {
       $attribute_code = $configurable['code'];
@@ -58,6 +59,7 @@ public function addToCartForm(array $form, FormStateInterface $form_state, SKU $
         '#title' => $configurable['label'],
         '#options' => $options,
         '#required' => TRUE,
+        '#weight' => $configurable_weights[$attribute_code],
         '#ajax' => [
           'callback' => [$this, 'configurableAjaxCallback'],
           'progress' => [
@@ -336,6 +338,13 @@ public function deriveProductTree(SKU $sku) {
       $tree['configurables'][$configurable['code']] = $configurable;
     }
 
+    $configurable_weights = \Drupal::service('config.factory')->get('acq_sku.configurable_weights')->get('weights');
+
+    // Sort configurables based on the config.
+    uasort($tree['configurables'], function ($a, $b) use ($configurable_weights) {
+      return $configurable_weights[$a['code']] - $configurable_weights[$b['code']];
+    });
+
     $tree['options'] = Configurable::recursiveConfigurableTree(
       $tree,
       $tree['configurables']
