diff --git a/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php b/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
index 6a3f47741..194f8d940 100644
--- a/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
+++ b/modules/acq_sku/src/Plugin/AcquiaCommerce/SKUType/Configurable.php
@@ -312,6 +312,12 @@ public function processImport($sku, array $product) {
    *   Configurables tree.
    */
   public function deriveProductTree(SKU $sku) {
+    static $cache = [];
+
+    if (isset($cache[$sku->language()->getId()], $cache[$sku->language()->getId()][$sku->id()])) {
+      return $cache[$sku->language()->getId()][$sku->id()];
+    }
+
     $tree = ['parent' => $sku];
 
     foreach ($sku->field_configured_skus as $child_sku) {
@@ -335,6 +341,8 @@ public function deriveProductTree(SKU $sku) {
       $tree['configurables']
     );
 
+    $cache[$sku->language()->getId()][$sku->id()] = $tree;
+
     return $tree;
   }
 
